[pytest]
# Mark custom
markers =
    e2e: End-to-end tests requiring live server and Ollama (skip in CI)

# Default: skip e2e unless explicitly requested
addopts = -m "not e2e"

# Output
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
