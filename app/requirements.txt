# AI Engineering Portfolio — Python dependencies
# Para arrancar todo el stack en Docker, este es lo unico que necesitas.

# Web framework
fastapi>=0.110
uvicorn[standard]>=0.27

# Validation
pydantic>=2.5

# HTTP client (uso interno + async)
httpx>=0.27

# Security
slowapi>=0.1.9  # rate limiting

# Data / viz
pandas>=2.0
matplotlib>=3.7
reportlab>=4.0  # PDF generation

# RAG
sentence-transformers>=2.2
qdrant-client>=1.7

# Testing
pytest>=8.0
pytest-asyncio>=0.23

# Note: torch, transformers, unsloth NO se incluyen aqui.
# Esos son solo para fine-tuning (phase-3), que requiere un venv separado
# con CUDA. Ver phase-3-finetuning/README.md.
